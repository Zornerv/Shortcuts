<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:enabled="true"
        android:icon="@drawable/ic_vaccine"
        android:shortcutDisabledMessage="@string/vaccine_disabled_label"
        android:shortcutId="vaccine"
        android:shortcutLongLabel="@string/vaccine_long_label"
        android:shortcutShortLabel="@string/vaccine_short_label">
        <intent
            android:action="android.intent.action.VIEW"
            android:data="shortcuts://fragment_b"
            android:targetClass="com.zornerv.shortcuts.MainActivity"
            android:targetPackage="com.zornerv.shortcuts" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />
        <capability-binding android:key="actions.intent.CREATE_MESSAGE" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>